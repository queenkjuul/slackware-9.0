CWD=`pwd`

VERSION=2.0.6
ARCH=noarch

cd /tmp
tar xjvf $CWD/gnome2-user-docs-$VERSION.tar.bz2
cd gnome2-user-docs-$VERSION
chown -R root.root .
find . -perm 666 -exec chmod 644 {} \;
find . -perm 664 -exec chmod 644 {} \;
CFLAGS="-O2 -march=i386 -mcpu=i686" \
./configure --prefix=/usr \
            --sysconfdir=/etc \
            --localstatedir=/var/lib \
            i386-slackware-linux
make
make install
mkdir -p /usr/doc/gnome2-user-docs-$VERSION
cp -a \
  AUTHORS BUGS COPYING HACKING MAINTAINERS NEWS README \
  /usr/doc/gnome2-user-docs-$VERSION
mkdir -p /install
cat $CWD/slack-desc > /install/slack-desc

